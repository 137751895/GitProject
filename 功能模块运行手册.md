# 功能模块运行手册

## 1. 运行目标与数据源

- 数据源：`data/klines/KQi@SHFEag/` 下本地CSV（1min/5min/15min）
- 检查方式：
  1) 主流程批量运行（特征工程→筛选→训练→回测→预测）
  2) 15min 专项链路（XGBoost预筛选→LSTM）
  3) 模块级烟测（features/models/backtest/tools）

---

## 2. 环境前置

- Python解释器：`D:/pyvenv/hfml/Scripts/python.exe`
- 关键依赖需可用：`lightgbm`、`xgboost`、`tensorflow`、`scikit-learn`、`optuna`、`bayesian-optimization`

---

## 3. 一键命令清单（按顺序）

### 3.1 特征阶段（真实CSV）

```bash
D:/pyvenv/hfml/Scripts/python.exe scripts/run_feature_phase.py
```

输出：
- `data/mx/feature_phase_summary.csv`
- `data/mx/feature_selection_report_1min.md`
- `data/mx/feature_selection_report_5min.md`
- `data/mx/feature_selection_report_15min.md`

### 3.2 全流程阶段（真实CSV）

```bash
D:/pyvenv/hfml/Scripts/python.exe scripts/run_full_pipeline_all_periods.py
```

输出：
- `data/mx/full_pipeline_summary.csv`
- `data/mx/backtest_report_1min.md`
- `data/mx/backtest_report_5min.md`
- `data/mx/backtest_report_15min.md`
- `data/mx/predictions_1min.csv`
- `data/mx/predictions_5min.csv`
- `data/mx/predictions_15min.csv`
- `data/mx/models/*`

### 3.3 15min 专项链路（强制 XGBoost→LSTM）

```bash
D:/pyvenv/hfml/Scripts/python.exe scripts/run_15min_lstm_chain.py --xgb-n-features 25 --max-rows 10000
```

输出：
- `data/mx/feature_selection_report_15min_lstm_chain.md`
- `data/mx/lstm_chain_summary_15min.md`
- `data/mx/predictions_15min.csv`

### 3.4 模块级烟测

```bash
D:/pyvenv/hfml/Scripts/python.exe scripts/run_module_smoke_checks.py
```

输出：
- `data/mx/module_smoke_results.csv`

### 3.5 实验日志更新

```bash
D:/pyvenv/hfml/Scripts/python.exe scripts/update_experiment_log.py --periods 1min,5min,15min --features-used full_pipeline --key-params "smoke_check" --notes "full project module check" --allow-empty
```

输出：
- `data/mx/experiment_log.csv`

### 3.6 调度脚本（UTF-8防乱码）

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\daily_batch_update.ps1 -Workspace . -Periods "1min,5min,15min" -Strict
```

输出：
- `logs/daily_batch_update_*.log`
- `logs/daily_batch_stdout_*.log`
- `logs/daily_batch_stderr_*.log`

说明：
- 脚本已启用 UTF-8 日志链路（`PYTHONUTF8=1`、`PYTHONIOENCODING=utf-8`）
- 调度脚本会在进程结束后将 `stdout/stderr` 日志统一归一化为 UTF-8，避免查看器乱码
- 脚本已启用单实例锁（`logs/daily_batch_update.lock`），同一时刻只允许一个批任务执行
- 失败判定以 Python 进程退出码为准（不是 stderr 文本本身）

---

## 4. 本次运行结果（已执行）

### 4.1 特征阶段汇总（`feature_phase_summary.csv`）

- 1min：`success`，样本 9218，特征 131，筛选后 40
- 5min：`success`，样本 9616，特征 123，筛选后 40
- 15min：`success`，样本 9677，特征 123，筛选后 25

### 4.2 全流程汇总（`full_pipeline_summary.csv`）

- 1min：`success`，accuracy=0.493854，f1=0.230769
- 5min：`success`，accuracy=0.523909，f1=0.619391
- 15min：本轮在 LSTM 训练阶段被 `KeyboardInterrupt` 中断（详见 `logs/real_data_pipeline_15min.log`）

### 4.3 15min 专项链路（`lstm_chain_summary_15min.md`）

- 样本数：9677
- 原始特征：123
- XGBoost筛选特征：25
- 预测行数：1432
- accuracy：0.572626
- f1_score：0.727516

### 4.4 模块烟测（`module_smoke_results.csv`）

- 通过模块：`load_real_data`、`feature_engineering`、`feature_selector`、`smart_labels`、`hybrid_trading`、`multi_timeframe`、`cost_model`、`var_stress`、`signal_postprocess`、`factor_loader`、`hyperopt_runner`、`adaptive_learning`
- 结果：全部 `PASS`

### 4.5 调度脚本快速回归记录（2026-02-12）

执行命令：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\daily_batch_update.ps1 -Workspace . -Periods "1min" -Strict
```

核验日志文件：
- `logs/daily_batch_update_20260212_173053.log`
- `logs/daily_batch_stdout_20260212_173053.log`
- `logs/daily_batch_stderr_20260212_173053.log`

核验结论：
- 调度主日志已记录启动与执行命令，解释器为项目虚拟环境 `D:/pyvenv/hfml/Scripts/python.exe`。
- stdout 日志可见主流程已进入数据加载、特征工程与特征筛选阶段。
- stderr 日志为空（当前无显式错误输出）。

### 4.6 乱码专项修复回归（2026-02-12 17:40）

执行命令：

```powershell
.\scripts\daily_batch_update.ps1 -Workspace . -Periods "1min" -Strict
```

核验日志文件：
- `logs/daily_batch_update_20260212_174020.log`
- `logs/daily_batch_stdout_20260212_174020.log`
- `logs/daily_batch_stderr_20260212_174020.log`

核验结论：
- `daily_batch_stdout_20260212_174020.log` 中中文日志已正常可读（如“商品期货ML量化模型”“数据加载与预处理”）。
- `stderr` 仍为空，当前未发现新增异常输出。
- 本次修复确认：乱码问题已收敛到可控范围，后续建议继续按 UTF-8 查看日志文件。

### 4.7 最新三周期回归记录（2026-02-12 17:57~17:58）

执行路径：
- `scripts/run_full_pipeline_all_periods.py`（单进程）
- `scripts/run_15min_lstm_chain.py --xgb-n-features 25 --max-rows 10000`（补齐15min专项）

核验结论：
- 1min、5min 全流程结果已刷新（`backtest_report_1min.md`、`backtest_report_5min.md` 更新时间 17:57）。
- 15min 在全流程脚本中训练阶段被中断，但专项链路已完整跑通并产出最新预测/总结（更新时间 17:58）。
- 最新 15min 专项指标：accuracy=0.572626，f1=0.727516。

---

## 5. 已发现问题（Bug/风险点）

### 5.1 `FirstProject.py` 运行失败（依赖缺失）

现象：
- 当前已改为友好提示并退出，不再直接崩溃。
- 根因仍是环境缺少 `vnpy` 依赖。

影响：
- VN Trader UI 启动功能不可用。

建议：
- 安装并配置 `vnpy` 全套依赖（含网关/交易环境）后再验证该入口。

### 5.2 调度脚本历史问题（已修复）

已修复内容：
- 异常日志现可输出完整细节与脚本堆栈。
- 调度脚本优先使用项目虚拟环境 Python，避免系统解释器依赖错位。
- 使用 UTF-8 输出链路，并对输出日志做 UTF-8 归一化处理，中文可读性已验证。
- 调度脚本新增并发保护锁，避免重复触发导致多个批处理进程互相干扰。
- 调度脚本已改为纯 ASCII 内容，避免 Windows PowerShell 在部分编码环境下误解析。

建议：
- 若仍出现乱码，优先检查终端代码页与编辑器文件编码设置。

### 5.4 并发触发风险（已加防护）

现象：
- 若短时间多次触发调度命令，可能出现多个 `update_experiment_log.py / run_real_data_pipeline.py` 并发执行。

影响：
- 资源竞争、日志混杂、结果文件时间戳不一致，影响回归结论可追踪性。

当前防护：
- `scripts/daily_batch_update.ps1` 已启用单实例锁；检测到已有运行任务时将直接阻止新任务启动。

### 5.3 TensorFlow告警（非阻塞）

现象：
- Keras 提示 `input_shape/input_dim` 传参方式建议调整。

影响：
- 不影响当前运行结果，但建议后续代码风格升级。

---

## 6. 15min 强制路线说明（当前已固化）

当前代码已强制：
- 15min 禁止跳过筛选
- 15min 筛选失败不回退全特征训练
- 15min 必须走：`XGBoost 预筛选 -> LSTM 训练`

涉及文件：
- `run_real_data_pipeline.py`
- `scripts/run_full_pipeline_all_periods.py`
- `scripts/run_feature_phase.py`

---

## 7. 常用结果文件定位

- 特征阶段：`data/mx/feature_phase_summary.csv`
- 全流程阶段：`data/mx/full_pipeline_summary.csv`
- 模块烟测：`data/mx/module_smoke_results.csv`
- 实验日志：`data/mx/experiment_log.csv`
- 运行日志目录：`logs/`

---

## 8. 建议的日常检查顺序

1) `run_feature_phase.py`（先验证数据和特征）
2) `run_full_pipeline_all_periods.py`（看训练回测输出）
3) `run_15min_lstm_chain.py`（专项链路）
4) `run_module_smoke_checks.py`（模块回归）
5) `update_experiment_log.py`（结果入库）
